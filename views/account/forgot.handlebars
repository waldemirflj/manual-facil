<div class="container">
  {{#if messages.info}}
    <div class="text-info">
      {{#each messages.info}}
        <div>{{msg}}</div>
      {{/each}}
    </div>
  {{/if}}
  {{#if messages.error}}
    <div class="text-danger">
      {{#each messages.error}}
        <div>{{msg}}</div>
      {{/each}}
    </div>
  {{/if}}
  <form class="form-signin col-md-4 offset-md-4 col-sm-8 offset-sm-2 mt-5" method="POST">
    <h4 class="h3 mb-3 font-weight-normal">Esqueceu sua senha?</h4>
    <p>Digite seu email e nós enviaremos as instruções para resetar a senha.</p>
    <label for="email" class="sr-only">Email</label>
    <input type="email" name="email" id="email" class="form-control" placeholder="Email" autofocus>
    <br>
    <button type="submit" class="btn btn-lg btn-black btn-block">Reset Senha</button>
  </form>
</div>