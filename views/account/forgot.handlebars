<style>
  .logo img {
    margin: 0 auto;
  }

  @media only screen and (min-width: 768px) {
    .logo img {
      width: 50% !important;
    }
  }

  @media only screen and (min-width: 1025px)  {
    .logo img {
      width: 35% !important;
    }
  }

  @media only screen and (max-width: 767px) {
    .logo img {
      width: 75% !important;
    }
  }
</style>

<div class="container text-center">
  {{#if messages.info}}
    <div class="text-info">
      {{#each messages.info}}
        <div>{{msg}}</div>
      {{/each}}
    </div>
  {{/if}}
  {{#if messages.error}}
    <div class="text-danger">
      {{#each messages.error}}
        <div>{{msg}}</div>
      {{/each}}
    </div>
  {{/if}}
</div>

{{> header}}

<div class="container text-center">
  <div class="row">
    <div class="col-12 mt-3 mb-3 logo">
      <img src="/images/logoFull.png" class="d-block" />
    </div>

    <div class="col-12">
      <div class="col-12 col-md-4" style="margin: 0 auto;">
        <form class="form-signin" method="POST">
          <h1 class="h3 mb-3 font-weight-normal">Esqueceu sua senha?</h1>
          <p>Digite seu email e nós enviaremos as instruções para resetar a senha.</p>
          <br>

          <label for="email" class="sr-only">Email</label>
          <input type="email" name="email" id="email" class="form-control" placeholder="Email" autofocus>
          <br>

          <button type="submit" class="btn btn-lg btn-black btn-block" style="background-color: #185285; font-weight: bold; color: #fff;">Recuperar Senha</button>
        </form>
      </div>
    </div>
  </div>
</div>
<br>

<script>
  document.getElementsByTagName('nav')[0].hidden = 'hidden'
</script>

